const cars: string[] = [];
// const plateNumber: number[] = [];
const plateNumber: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const users: User[] = [];

const MLModels: number[][][] = [[[]]];

users.push({ name: 'Tima', isActive: true });

cars.push('BMW');
plateNumber.push(123);
